name: Release

# On every version tag (vMAJOR.MINOR.PATCH) this workflow verifies the code,
# cross-compiles the Linux and macOS binaries, generates SHA-256 checksums and
# publishes a GitHub release with the CHANGELOG section as the notes.

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

# Least privilege by default; the job below elevates only what it needs.
permissions:
  contents: read

jobs:
  release:
    name: Build and publish
    runs-on: ubuntu-latest
    permissions:
      contents: write # create the release and upload assets
      id-token: write # keyless signing / build provenance (OIDC)
      attestations: write # record the provenance attestation
    steps:
      - name: Check out
        uses: actions/checkout@v4
        with:
          # Full history + tags so `git describe` stamps the exact version.
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          check-latest: true

      - name: Verify (vet, gofmt, tests)
        run: make check

      - name: Cross-compile release binaries
        run: make release

      - name: Generate checksums
        working-directory: dist
        run: sha256sum ledgerline-cli-* > checksums.txt

      - name: Attest build provenance
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: dist/ledgerline-cli-*

      - name: Install cosign
        uses: sigstore/cosign-installer@v3

      - name: Sign checksums (keyless)
        working-directory: dist
        run: |
          cosign sign-blob --yes checksums.txt \
            --output-signature checksums.txt.sig \
            --output-certificate checksums.txt.pem

      - name: Extract release notes from CHANGELOG
        env:
          TAG: ${{ github.ref_name }}
        run: |
          version="${TAG#v}"
          awk -v ver="$version" '
            $0 ~ "^## \\[" ver "\\]" { flag = 1; next }
            flag && /^## \[/ { exit }
            flag { print }
          ' CHANGELOG.md > release-notes.md
          if [ ! -s release-notes.md ]; then
            printf 'Release %s\n' "$TAG" > release-notes.md
          fi

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.ref_name }}
        run: |
          gh release create "$TAG" \
            --title "$TAG" \
            --notes-file release-notes.md \
            --verify-tag \
            dist/ledgerline-cli-* dist/checksums.txt \
            dist/checksums.txt.sig dist/checksums.txt.pem
